# swift-protobuf-binary
Builds binaries of protoc-gen-swift until such time as Apple sees fit to do it for us. 

To make a new version available, create a release with a tag that matches one found in [Apple's releases](https://github.com/apple/swift-protobuf/releases).
